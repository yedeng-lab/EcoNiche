% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca.R
\name{cca_calc_species}
\alias{cca_calc_species}
\title{Calculate Species Niche Width and Position (Step 3)}
\usage{
cca_calc_species(
  otu,
  site_width,
  site_pos,
  method = c("lm", "loess"),
  make_plot = TRUE,
  top_node = 10000
)
}
\arguments{
\item{otu}{OTU/species matrix (rows = taxa, columns = samples).}

\item{site_width}{Site scores for width axis (usually matrix from partial CCA).}

\item{site_pos}{Site scores for position axis (usually vector from CCA axis 1).}

\item{method}{Smoothing method for the plot ("lm" or "loess").}

\item{make_plot}{Logical; if \code{TRUE}, returns a ggplot object.}

\item{top_node}{Integer; maximum number of most abundant species to use for 
loess smoothing calculation (default 10000). Points are plotted for all valid species.}
}
\value{
A list containing:
\describe{
  \item{species}{Data frame of species traits (NichePosition, NicheWidth).}
  \item{plot}{The ggplot object (if \code{make_plot = TRUE}).}
}
}
\description{
Computes the niche width and niche position for each species using the site scores
from Step 2. Generates a plot of Niche Width vs. Niche Position.
}
