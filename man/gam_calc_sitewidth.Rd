% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gam.R
\name{gam_calc_sitewidth}
\alias{gam_calc_sitewidth}
\title{Calculate Sample-Level Niche Width Weighted by Abundance}
\usage{
gam_calc_sitewidth(
  otu,
  niche_df,
  otu_col = "OTU",
  width_col = "breadth50",
  weight_mode = c("auto", "counts", "relative")
)
}
\arguments{
\item{otu}{An OTU-by-sample matrix or data frame.}

\item{niche_df}{A data frame containing at least columns for OTU IDs and niche width values.}

\item{otu_col}{Character string; the column name in \code{niche_df} containing OTU IDs. 
Default is \code{"OTU"}.}

\item{width_col}{Character string; the column name in \code{niche_df} containing niche width values. 
Default is \code{"breadth50"}.}

\item{weight_mode}{Character string; how to handle abundance weights. One of \code{"auto"}, 
\code{"counts"}, or \code{"relative"}.}
}
\value{
A data frame with columns \code{sample} and \code{Bw50_abundance_weighted}.
}
\description{
Computes the abundance-weighted mean niche breadth for each sample, given an 
OTU-by-sample table and OTU-level niche breadth estimates (e.g., from \code{gam_fit_model}).
The formula is:
\deqn{B_j = \sum_i (abundance_{ij} \times breadth50_i) / \sum_i abundance_{ij}}
}
