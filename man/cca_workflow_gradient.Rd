% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca.R
\name{cca_workflow_gradient}
\alias{cca_workflow_gradient}
\title{Execute Gradient-Based Workflow (Steps 1-4)}
\usage{
cca_workflow_gradient(
  otu,
  env,
  sel,
  covariates,
  standardize = TRUE,
  method = c("lm", "loess"),
  var = sel[1],
  galaxy_colnum = TRUE,
  make_plot = TRUE,
  top_node = 10000
)
}
\arguments{
\item{otu}{An OTU/species matrix or data frame (rows = taxa, columns = samples).}

\item{env}{A data.frame of environmental variables. 
Rows must be sample IDs and columns must be environmental variables.}

\item{sel}{A vector of variable names or column indices to be used as constrained variables.}

\item{covariates}{Variables (names or indices) defining the position axis (used in CCA and as covariates in partial CCA).}

\item{standardize}{Logical; if \code{TRUE}, environmental variables are standardized 
(centered and scaled) column-wise. Default is \code{TRUE}.}

\item{method}{Smoothing method for the plot ("lm" or "loess").}

\item{var}{Environmental variable for gradient plotting (passed to Step 4).}

\item{galaxy_colnum}{Logical; if \code{TRUE} and numeric indices are provided, 
they are treated as 1-based indices where column 1 is assumed to be SampleID 
(and thus indices are decremented by 1). Default is \code{TRUE}.}

\item{make_plot}{Logical; if \code{TRUE}, returns a ggplot object.}

\item{top_node}{Integer; maximum number of most abundant species to use for 
loess smoothing calculation (default 10000). Points are plotted for all valid species.}
}
\value{
A named list containing outputs of steps 1 through 4.
}
\description{
Runs the complete environmental gradient analysis pipeline:
1. Prepare env data (PCA)
2. Fit CCA/Partial-CCA
3. Calculate species niche traits
4. Analyze gradient vs niche position
}
