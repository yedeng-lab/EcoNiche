% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca.R
\name{cca_workflow_group}
\alias{cca_workflow_group}
\title{Execute Group-Based Workflow (Steps 1-2, 5-6)}
\usage{
cca_workflow_group(
  otu,
  env,
  sel,
  group,
  covariates,
  standardize = TRUE,
  method = c("lm", "loess"),
  var = sel[1],
  choice = "all",
  galaxy_colnum = TRUE,
  make_plot = TRUE,
  plot_type = c("sample", "group", "both"),
  show_ci = TRUE,
  annotate_stats = TRUE
)
}
\arguments{
\item{otu}{An OTU/species matrix or data frame (rows = taxa, columns = samples).}

\item{env}{A data.frame of environmental variables. 
Rows must be sample IDs and columns must be environmental variables.}

\item{sel}{A vector of variable names or column indices to be used as constrained variables.}

\item{group}{A named vector or factor defining groups. Names must match sample IDs.}

\item{covariates}{Variables (names or indices) defining the position axis (used in CCA and as covariates in partial CCA).}

\item{standardize}{Logical; if \code{TRUE}, environmental variables are standardized 
(centered and scaled) column-wise. Default is \code{TRUE}.}

\item{method}{Smoothing method ("lm" or "loess").}

\item{var}{Environmental variable for gradient plotting (passed to Step 6).}

\item{choice}{Calculation mode. If \code{"all"}, computes niche width using all samples. 
Otherwise, computes within-group niche widths.}

\item{galaxy_colnum}{Logical; if \code{TRUE} and numeric indices are provided, 
they are treated as 1-based indices where column 1 is assumed to be SampleID 
(and thus indices are decremented by 1). Default is \code{TRUE}.}

\item{make_plot}{Logical; if \code{TRUE}, returns ggplot object(s).}

\item{plot_type}{Type of plot: "sample", "group", or "both".}

\item{show_ci}{Logical; whether to show confidence intervals on the smooth line.}

\item{annotate_stats}{Logical; whether to annotate R-squared and P-value on the plot.}
}
\value{
A named list containing outputs of steps 1, 2, 5, and 6.
}
\description{
Runs the complete group-based analysis pipeline:
1. Prepare env data (PCA)
2. Fit CCA/Partial-CCA
5. Calculate group niche widths
6. Plot group/sample niche width vs gradient
}
